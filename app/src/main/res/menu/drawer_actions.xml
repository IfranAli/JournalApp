<menu xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_addBook"
        android:icon="@android:drawable/ic_input_add"
        android:title="add"
        android:showAsAction="ifRoom" />
</menu>